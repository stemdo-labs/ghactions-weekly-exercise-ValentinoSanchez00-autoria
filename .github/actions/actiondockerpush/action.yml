# .github/actions/actiondockerpush/action.yml
name: Docker Push Action
description: "Push Docker image to DockerHub"
inputs:
  image_name:
    description: "The Docker image name"
    required: true
  version:
    description: "Version tag for the Docker image"
    required: true

runs:
  using: "composite"
  steps:
    - name: Generate full image tags
      id: generate_full_image_tags
      run: |
        full_tags="${{ secrets.USERNAME }}/${{ inputs.image_name }}:${{ inputs.version }}"
        echo "full_image_tags=$full_tags" >> $GITHUB_ENV  
      shell: bash
